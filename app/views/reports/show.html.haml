%h1= @report.exc_name

%ul
	%li= @report.exc_reason
	%li= @report.device
	%li= @report.os_version
	%li= @report.app_version
	%li= @report.count

%textarea{ :rows => "20", :cols => "100" }
	= @report.backtrace
	