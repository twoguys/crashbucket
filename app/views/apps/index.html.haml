%h2{ :style => "margin-left:50px;display:inline;" } Apps
:plain
  &nbsp;&bull;&nbsp;
= link_to "New App", new_app_path
%table
  %tr{ :id => "table_header" }
    %th{ :class => "first app_status" }
    %th{ :class => "app_name" } name
    %th{ :class => "app_key" } api key
    %th{ :class => "last app_reports" } crash reports
  - @apps.each do |app|
    %tr{ :class => "table_row" }
      %td
      %td= link_to app.name, app
      %td= app.api_key
      %td
        - count = app.reports.count
        - if count == 0
          no reports
        - elsif count == 1
          1 report
        - else
          = "#{count} reports"
